apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: homelab994-prod
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/homelab994/gh-newops
        revision: HEAD
        directories:
          - path: charts/*

  template:
    metadata:
      name: '{{path.basename}}-prod'

    spec:
      project: homelab994

      source:
        repoURL: https://github.com/homelab994/gh-newops
        targetRevision: HEAD
        path: '{{path}}'

        helm:
          valueFiles:
            - ../../environments/prod/{{path.basename}}-values.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: prod

      syncPolicy:
        automated:
          prune: true
          selfHeal: true